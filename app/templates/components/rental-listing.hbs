<li>{{rental.owner}}'s {{rental.type}} in {{rental.city}}
  {{#if isImageShowing }}
    <p><img src="{{rental.image}}" alt="{{rental.type}}" width="500" {{action "imageHide"}}/></p>
    <!-- <button {{action "imageHide"}}>Hide Image</button> -->
  {{else}}
    <button {{action "imageShow"}}>Show Image</button>
  {{/if}}
    <button {{action 'delete' rental}}>Delete</button>
</li>
